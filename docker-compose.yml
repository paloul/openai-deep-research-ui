services:
  open-deep-research:
    build: .
    container_name: open-deep-research
    ports:
      - "443:443"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('https://localhost:443/', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
